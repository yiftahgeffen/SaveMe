<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hackathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name="com.example.saveMe.WillTakeCareActivity"></activity>
        <activity android:name="com.example.saveMe.WearablesActivity" />
        <activity android:name="com.example.saveMe.WearableActivity" />
        <activity android:name="com.example.saveMe.EmergencyContacts" />
        <activity
            android:name="com.example.saveMe.TripActivity"
            android:label="@string/trip_name"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name="com.example.saveMe.MyTripsActivity" />
        <activity android:name="com.example.saveMe.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>